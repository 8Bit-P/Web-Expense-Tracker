import React from "react";
import { Box } from "@chakra-ui/react";

const GeneralStatisticsGraph = () => {
  /* TODO: Dark rectangle with multiple circles that show weekly,monthly and yearly spent */
  /* ANIMATION WHERE IT STARTS AS A SQUARE AND SCALES TO SHAPE FROM TOP LEFT TO RIGHT BOTTOM */
  return (
    <Box
      w="35%"
      maxW="600px"
      h="300px"
      bgColor="blackAlpha.600"
      borderRadius={"30px"}
    ></Box>
  );
};

export default GeneralStatisticsGraph;
